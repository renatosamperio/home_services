torrent_crawler: {
  page_limit: 1000,
  search_type: browse-movies,
  with_magnet: true,
  with_db: true,
  
}

torrent_finder: {
  list_term: /valid/path/list_termx.txt,
  slack_token: invalid-token-set-corrent-one,
  slack_channel: latest_torrents,
  latest_days: 3
}

imdb_collector: {
  database: imdb,
  collection: movies,
  list_term: /valid/path/
}